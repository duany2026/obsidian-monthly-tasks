# 月历任务插件 (Monthly Tasks)

一款为 Obsidian 打造的滴答清单风格月视图任务管理插件，支持农历显示、节假日标注和调休信息展示。

---

## 功能特性

### 核心功能
- **月视图日历** - 清晰的 5-6 周动态网格布局，查看每月任务一目了然
- **任务管理** - 快速创建、完成、删除任务
- **优先级标记** - 🔴 高优先级 / 🟡 中优先级 / 普通
- **跨天任务** - 多日任务在起止日期内的每一天自动显示
- **农历显示** - 自动显示中国传统农历日期

### 节假日
- **法定节假日标注** - 春节、国庆、五一劳动节等用红色背景高亮
- **调休日标注** - 周末上班日用蓝色背景标注
- **按需数据更新** - 浏览到未缓存年份时自动按需获取（holiday-cn / timor.tech）；启动预取与手动刷新可选

### 用户体验
- **深色模式** - 自动适配 Obsidian 主题
- **移动端适配** - 响应式设计，手机平板都能用
- **操作简便** - 点击日期即可添加任务，支持命令面板操作

---

## 安装

### 方式一：BRAT

1. 安装 [BRAT](https://obsidian.md/plugins?id=obsidian42-brat) 插件
2. 在 BRAT 设置中添加本插件的 GitHub 仓库地址（发布到 GitHub 后填写实际地址）
3. 启用插件即可

### 方式二：手动安装

1. 下载最新版本的 `monthly-tasks-1.4.0.zip`
2. 解压到 Obsidian 插件目录 `.obsidian/plugins/monthly-tasks/`
3. 重启 Obsidian 并启用插件

---

## 使用方法

### 打开月历视图

1. 点击左侧边栏的日历图标
2. 或使用命令面板（`Ctrl/Cmd + P`）搜索「月历任务」
3. 选择「打开月历任务视图」

### 添加任务

1. 点击任意日期格子
2. 在弹窗中输入任务内容
3. 可选择：
   - 📅 **截止日期** - 默认当天
   - ⏰ **时间范围** - 非全天任务可设置起止时间（需在设置中关闭「默认全天任务」才会显示时间输入）
   - 🎨 **优先级** - 高/中/普通
   - 🛫 **跨天任务** - 设置开始日期后，任务在起止日期内的每一天重复显示（格子内以蓝色渐变条标识，首日带续接箭头）
4. 按 **Enter** 键或点击确认按钮创建任务

### 任务格式

任务以标准 Obsidian checklist 格式存储：

```markdown
- [ ] 完成任务 📅 2026-04-22
- [x] 已完成任务 📅 2026-04-21
- [ ] 跨天任务 🛫 2026-04-22 📅 2026-04-25
- [ ] 带时间的任务 ⏰ 09:00~12:00 📅 2026-04-22
- [ ] 高优先级任务 🔴 📅 2026-04-22
- [ ] 中优先级任务 🟡 📅 2026-04-22
```

任务自动保存到 `任务/YYYY年任务列表.md` 文件中（统一归集，不写入日记文件）。

---

## 设置选项

| 设置项 | 说明 | 默认值 |
|--------|------|--------|
| 显示已完成任务 | 在日历中显示已完成任务 | ✅ 开启 |
| 已完成隐藏删除线 | 打开后已完成任务将隐藏删除线 | ✅ 开启 |
| 默认全天任务 | 新建任务默认为全天 | ✅ 开启 |
| 每周第一天 | 周日或周一 | 周日 |
| 显示农历 | 显示中国传统农历 | ✅ 开启 |
| 显示节假日 | 显示法定节假日和调休 | ✅ 开启 |
| 每日任务上限 | 每天最多显示的任务数 | 5 |
| 任务文件夹 | 选择任务文件的存储位置 | 默认（任务） |
| 启动时自动刷新节假日 | 启用插件时自动从 holiday-cn / timor.tech 获取最新数据 | ❌ 关闭 |
| 刷新节假日数据 | 从网络获取最新节假日 | 按钮 |

---

## 数据存储

插件数据存储在插件设置中：
- `data.json` - 用户设置和节假日缓存
- `任务/YYYY年任务列表.md` - 任务内容文件

---

## 节假日数据

### 数据获取链
按顺序尝试，任一源成功即采用：

1. **[holiday-cn](https://github.com/NateScarlet/holiday-cn)** — 社区维护，跟随国务院公告及时更新，覆盖 **2007 年至今**（经 jsDelivr CDN 访问）
2. **[timor.tech](https://timor.tech)** — 备份数据源
3. **内置兜底** — 插件目录 `holidays.json`（2022–2026 官方数据，离线可用）

### 内置兜底数据（holidays.json）
- 随插件分发，无网络时仍可显示已内置年份；官方未发布的年份不写预测数据
- **数据与代码分离**：更新内置数据只需替换插件目录下的 `holidays.json`，无需改代码。格式：

```json
{
  "2026": [
    { "date": "2026-01-01", "name": "元旦", "type": "legal", "isOff": true },
    { "date": "2026-05-09", "name": "班", "type": "workday", "isOff": false }
  ]
}
```

- 内置数据不再逐年维护：新年份由上面的数据源自动覆盖

### 使用方式
- 浏览到未缓存年份（如 2007 年起的历史年份、未来新年份）时会自动按需获取该年数据
- 「启动时自动刷新节假日」默认关闭，仅控制启动时是否预取今年前后三年；也可手动点击「刷新节假日数据」强制刷新今年前后三年
- 数据获取失败按 5 分钟退避，期间浏览不重复请求

---

## 界面预览

```
┌─────────────────────────────────────────────┐
│  ◀  2026年4月  ▶                            │
├─────┬─────┬─────┬─────┬─────┬─────┬─────┤
│ 日  │ 一  │ 二  │ 三  │ 四  │ 五  │ 六  │
├─────┼─────┼─────┼─────┼─────┼─────┼─────┤
│     │     │     │  1  │  2  │  3  │  4  │
│     │     │     │     │     │ 清明 │ 清明 │
├─────┼─────┼─────┼─────┼─────┼─────┼─────┤
│  5  │  6  │  7  │  8  │  9  │ 10  │ 11  │
│ 清明│     │     │     │     │ 班  │     │
├─────┼─────┼─────┼─────┼─────┼─────┼─────┤
│ ...                                        │
└─────────────────────────────────────────────┘
```

---

## 技术架构

### 模块结构

| 模块 | 说明 |
|------|------|
| TaskModel | 任务数据模型，包含任务解析、优先级提取等工具函数 |
| TaskParser | 任务解析器，负责解析 Obsidian 库中的任务，管理缓存 |
| Calendar | 日历工具，生成月历网格数据 |
| LunarCalendar | 农历转换，支持 1900-2100 年的农历日期转换 |
| HolidayManager | 节假日管理器，管理法定节假日和调休信息 |
| MonthlyView | 月历视图，渲染日历网格和任务列表 |
| CreateTaskModal | 创建任务弹窗，处理任务创建表单 |
| DatePickerModal | 日期选择器弹窗，支持快速切换年月 |
| MonthlyTasksPlugin | 插件主类，管理插件生命周期 |
| MonthlyTasksSettingTab | 设置界面，提供用户配置选项 |

### 技术栈

- **语言**: JavaScript（`main.js` 为唯一源码，直接手工维护，无构建步骤）
- **框架**: Obsidian API
- **样式**: CSS（移动端优先设计）
- **数据**: 数据源链（holiday-cn → timor.tech）+ `holidays.json` 内置兜底

---

## 更新日志

查看完整的版本更新历史，请参阅 [CHANGELOG.md](./CHANGELOG.md)。

### 最新版本 v1.4.0 (2026-09-05)

**移动端**
- 创建任务弹窗改用 Obsidian Modal 基类：系统返回键、ESC、点遮罩均可关闭；打开弹窗不再自动拉起输入法
- 适配新版弹窗结构：去掉多余的 X 按钮与顶部空白；触屏上悬停效果不再粘滞，「已有任务」的删除/跳转按钮常显

**修复与打磨**
- 中文输入法按 Enter 确认候选词不再误提交任务；仅有 🛫 无 📅 的跨天任务不再静默消失
- 「今天」格子外框收敛；暗色模式对比度与一致性修复；弹窗背景改用主题变量，兼容亮色自定义主题
- 发布前复审：浏览到未缓存年份（2022-2026 以外）时月历不再被网络请求阻塞（先渲染、取回后补刷一次，离线不再卡顿最长约 24.5 秒）；弹窗框架装饰清理移除类名正则启发式扫描；README 联网口径更正（默认不预取，仅浏览未内置年份时按需请求）

完整变更见 [CHANGELOG.md](./CHANGELOG.md)。1.3.7–1.3.9 为未发布的内部迭代，已并入 1.4.0。

### v1.3.6 (2026-09-04)

**修复**
- 倒序建月（如先建 3 月任务再建 2 月）不再产生连续两个 `---` 分隔符：插入点前已有分隔符时复用
- 倒序建月后新月份与后续月份之间补齐 `---` 分界，向早月追加任务不再"串"进下一月份的 section
- 新建月份 section 时空行按需补齐，不再在头部规则线前累积冗余空行

### v1.3.5 (2026-09-04)

**修复**
- 切换到其他视图再切回时月历不再左右抖动：重挂载瞬间滚动条闪现不再挤压网格宽度（固定预留滚动条槽位）
- 容器高度口径修正（`box-sizing: border-box`），消除恒超出 view-content 24px 的幽灵溢出

**内部**
- 清理 styles.css 死样式 138 行（main.js 交叉引用核验，无功能影响）

### v1.3.4 (2026-09-03)

**优化**
- 移除每次新增任务与勾选完成时的成功提示（任务出现在日历中、勾选框状态变化本身就是反馈）；所有失败提示保留

### v1.3.3 (2026-09-02)

**修复**
- 侧栏矮面板下月历网格不再被裁切：5 周月动态行数 + 容器可滚动
- 创建任务写入保持 CRLF/LF 行尾一致与 Markdown 结构完整
- 弹窗内日期选择浮层不再被模态框裁切
- 非法日期、⏰ 时间垃圾值改为显式提示/丢弃，不再静默参与逻辑
- 弹窗任务计数按所选日期过滤；跨天区间超 366 天仅挂载首尾日
- 设置加载全字段校验；卸载时清理日期选择器

**优化**
- `metadataCache` 预筛覆盖无 `listItems` 文件，减少无谓全文读取
- 内置节假日数据允许会话内一次网络升级，落实「API 优先」

[查看完整更新日志 →](./CHANGELOG.md)

---

## 发布打包

打包发布时只包含 `main.js`、`manifest.json`、`styles.css`、`holidays.json`（可选 `versions.json`），**不要携带本地 `data.json`**——其中存有作者本机的设置（如节假日自动刷新开关）与节假日缓存，会让新用户安装后即继承作者设置、启动就联网预取。插件的联网口径是：**默认不预取**（启动预取由「启动时自动刷新」控制，默认关）；浏览到未内置年份（2022-2026 以外）时会**按需**向 holiday-cn / timor.tech 请求该年数据。

---

## License

MIT License - 详见 [LICENSE](./LICENSE) 文件

---

## 贡献

欢迎提交 Issue 和 Pull Request！
